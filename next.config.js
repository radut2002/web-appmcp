/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.ctfassets.net', 'example.com', 'localhost', 'i.pravatar.cc/150?img=1', 'i.pravatar.cc'],
  },
}

module.exports = nextConfig
